---
title: "Coral Demography Data Cleaning"
format: html
editor: visual
---

```{r}
library(tidyverse)
library(readxl)
library(here)
library(janitor)

wide_data_2013_2023 <- read_excel(here("rawdata", "Coral_Deomography_allyears_2013-24_V1.xlsx"), sheet = 1)

individual_files <- list.files("rawdata/2024sheets", pattern = "*.xlsx", full.names = TRUE)

read_file <- function(f) {
  meta <- read_excel(f, col_names = FALSE, n_max = 1)
  obs <- tail(na.omit(as.character(meta[1, ])), 1)
  read_excel(f, skip = 1, col_names = TRUE, col_types = "text") %>%
    mutate(Observer = obs)
}

df <- bind_rows(lapply(individual_files, read_file)) %>%
    select(-c("W", "H", "L", "Status"))  %>%
  rename(habitat = Hab,
    transect = Tran) %>%
  mutate(l = coalesce(L2024, L24),
         w = coalesce(W2024, W24),
         h = coalesce(H2024, H24),
         dynamics = coalesce(Dynam, Dynamic, Dyn, Dynamics),
         across(c(X, Y, Z, l, w, h), as.numeric)
  ) %>%
  select(-any_of(c("L2024", "W2024", "H2024", "L24", "W24", "H24",
                   "Dynam", "Dynamic", "Dyn", "Dynamics", "...17",
                   "status", "source_file"))) %>%
  mutate(year = 2024) %>% 
  clean_names()
```


```{r}
wide_data_2013_2023 %>% 
  pivot_longer(c("W2013", "W2014"), names_to = "Year", values_to = c("w"))
```

```{r}
long_data <- wide_data_2013_2023 %>%
  select(!contains("24")) %>% 
  pivot_longer(cols = -c(1:8), names_to = c(".value", "year"), names_pattern = "(.*)(\\d{4}$)") %>% 
  filter(!is.na(year))
```

```{r}
long_data_t <- long_data %>% 
  filter(str_detect(transect, "T")) %>% 
  mutate(present = case_when(
    str_detect(dynamics_, "Death") ~ 1,
    str_detect(dynamics_, "Recruitment") ~ 1,
    TRUE ~ 0
  ))

```


```{r}
pivot_test2 <- long_data_t %>% 
  separate_rows(dynamics_, sep = ",") %>% 
  mutate(other_dyn = dynamics_, value = 1) %>% 
  pivot_wider(names_from = dynamics_, values_from = present, names_prefix = "dyn_")
```

